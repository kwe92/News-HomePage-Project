import React from "react";
import { StyledButton, ButtonBody } from "./ReadMoreButtonStyles";

const ReadMoreButton = (props:{}) => {
    return (
        <StyledButton>
            <ButtonBody>
            read more
            </ButtonBody>
        </StyledButton>
    );
};

export default ReadMoreButton;