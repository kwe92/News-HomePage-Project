import React from "react";
import { BottomSectionMainContainer } from "./BottomSectionStyles";

const BottomSection = (props:{}) => {
    return (
        <BottomSectionMainContainer>
            Bottom Section
        </BottomSectionMainContainer>
    );
};

export default BottomSection;